{% extends 'base_no_nav.html.twig' %}

{% block body %}

    <style>
        body {
            padding-top: 0;
        !important
        }
    </style>

    <div class="row login">

        <div class="container">
            {#{% if error %}
                <div>{{ error.messageKey|trans(error.messageData, 'security') }}</div>
            {% endif %}#}

            <div class="col-sm-12">
                <div class="form">
                    <h2>Title</h2>
                    {% for flash_message in app.session.flashBag.get('notice') %}
                        <div class="flash-message">
                            <p>{{ flash_message }} </p>

                        </div>
                    {% endfor %}

                    <p><a href="{{ path('user_registration') }}">Or click here to create a new account</a></p>

                </div>
            </div>

        </div>

    </div>

{% endblock %}



