{% extends 'base_no_nav.html.twig' %}

{% block body %}

    <table class="table">
        {% for user in users %}
            <tr>
                <td>{{ user.username }}</td>
                <td>{{ user.email }}</td>

            </tr>
        {% endfor %}
    </table>


{% endblock body %}